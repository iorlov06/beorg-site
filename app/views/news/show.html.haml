-# Рендер одной новости
%h2
  =@article.title
%p
  =@article.description
-if current_user.role > 0
  %p
    =link_to 'Edit', "/news/#{@article.id}/edit"
  %p
    =link_to 'Delete', "/news/#{@article.id}", method: :delete, confirm: 'Are you sure want to delete this article?'
